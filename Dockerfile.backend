# Use official Python lightweight image
FROM python:3.12-slim

# Install system dependencies (e.g. required for building psycopg if necessary)
RUN apt-get update && apt-get install -y libpq-dev gcc && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /app

# Install the ultra-fast 'uv' package manager
RUN pip install uv

# Copy application files
COPY . .

# Install Python dependencies natively
RUN uv sync

# Cloud Run injects the PORT environment variable (default 8080)
ENV PORT=8080
EXPOSE 8080

# Start FastAPI server using uvicorn, binding to the Cloud Run PORT variable
CMD ["sh", "-c", "uv run uvicorn api:app --host 0.0.0.0 --port ${PORT:-8080}"]
